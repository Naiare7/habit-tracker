/**
 * Paquete dto (Data Transfer Object).
 * <p>
 * Contiene los objetos que se usan para transferir datos entre el frontend y el backend.
 * Los DTOs de entrada reciben los datos de las peticiones HTTP.
 * Los DTOs de salida definen la estructura JSON que se devuelve al frontend.
 * No incluyen campos sensibles como contrasenas.
 */
package com.habittracker.dto;
